export default class GameEntity {
  constructor(x, y, width, height, sprite, alpha) {
    this.x = x;
    this.y = y;
    this.rotation = 0
    this.width = width;
    this.height = height;
    this.sprite = sprite;
    this.alpha = alpha
  }

  draw() {

  }
}